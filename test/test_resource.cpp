#include <catch2/catch_test_macros.hpp>


#include "resources.h"


TEST_CASE("vaccel_resource_bootstrap", "[vaccel_resource]")
{   
    // int ret = resources_bootstrap();
    // REQUIRE(ret == VACCEL_OK);

    /// I don't know why this doesn't work.
    
    REQUIRE(1==1);
    
}